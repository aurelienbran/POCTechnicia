# .env.example pour Technicia Assistant
# Copiez ce fichier vers .env et modifiez les valeurs selon votre environnement

# ----------------------
# API Keys
# ----------------------
# Clé API Anthropic pour Claude 3.5 Sonnet (requis)
ANTHROPIC_API_KEY=your_anthropic_api_key

# Clé API Voyage AI pour la génération d'embeddings (requis)
VOYAGE_API_KEY=your_voyage_api_key

# ----------------------
# Configuration de la base vectorielle
# ----------------------
# Hôte Qdrant (défaut: localhost)
QDRANT_HOST=localhost

# Port Qdrant (défaut: 6333)
QDRANT_PORT=6333

# Nom de la collection de documents (défaut: documents)
COLLECTION_NAME=documents

# ----------------------
# Configuration de l'application
# ----------------------
# Taille maximale des fichiers à télécharger en octets (défaut: 150 MB)
MAX_UPLOAD_SIZE=157286400  # 150 MB

# Taille des chunks pour le découpage des documents (défaut: 1000)
CHUNK_SIZE=1000

# Chevauchement des chunks en nombre de caractères (défaut: 200)
CHUNK_OVERLAP=200

# Limite de mémoire en MB pour les processus (défaut: 1024 MB)
MAX_MEMORY_MB=1024

# ----------------------
# Configuration du serveur
# ----------------------
# Adresse d'écoute du serveur (défaut: 0.0.0.0 pour toutes les interfaces)
HOST=0.0.0.0

# Port d'écoute du serveur (défaut: 8000)
PORT=8000

# Mode debug (true/false)
DEBUG=true

# ----------------------
# Configuration de l'installation portable
# ----------------------
# Ces variables sont prévues pour une utilisation future avec l'installation portable
# Elles ne sont pas encore implémentées dans le code actuel, mais seront utilisées
# par les scripts portables générés par portable_installer.bat
#PYTHON_PORTABLE_PATH=../portable_deps/python_portable
#NODE_PORTABLE_PATH=../portable_deps/nodejs_portable
#QDRANT_PORTABLE_PATH=../portable_deps/qdrant_portable
#USE_PORTABLE_DEPS=true

# ----------------------
# Configuration OCR
# ----------------------
# Chemins des dépendances OCR (optionnels)
# Si non spécifiés, l'application cherchera dans le PATH système

# Chemin vers l'installation Tesseract OCR
TESSERACT_PATH=C:\Program Files\Tesseract-OCR

# Chemin vers l'installation Poppler (outils PDF)
POPPLER_PATH=C:\Program Files\poppler\bin

# Chemin vers l'installation Ghostscript
GHOSTSCRIPT_PATH=C:\Program Files\gs\bin
